<view class="container">
  <open-data class="avatar" type="userAvatarUrl"></open-data>
  <van-button type="info" size="large" open-type="getUserInfo" bindgetuserinfo="onLogin" bind:tap="logout">
    {{user.unionId ? '退出登录' : '授权登录'}}
  </van-button>
</view>